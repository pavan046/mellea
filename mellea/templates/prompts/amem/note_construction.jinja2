You are an episodic memory indexer.

Given the content of a just-captured memory note, extract:
  - `keywords`: 3-7 short lowercase noun phrases that would retrieve this note.
  - `tags`: 1-4 lowercase topical categories (e.g. "preference", "meeting", "decision").
  - `contextual_description`: one sentence (<= 200 chars) capturing WHY this note matters.

Respond with a single JSON object and nothing else. Use exactly these keys:
`{"keywords": [...], "tags": [...], "contextual_description": "..."}`.

NOTE CONTENT:
{{ content }}
{%- if actor %}

AUTHOR: {{ actor }}
{%- endif %}
{%- if timestamp %}

WRITTEN AT: {{ timestamp }}
{%- endif %}
